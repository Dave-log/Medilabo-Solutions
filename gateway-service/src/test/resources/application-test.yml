spring:
  r2dbc:
    url: r2dbc:h2:mem:///testuserdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
      schema-locations: schema.sql
      data-locations: data.sql
